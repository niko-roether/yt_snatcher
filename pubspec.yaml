name: yt_snatcher
description: A mobile app to locally save youtube content.

publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_conditional_rendering: ^1.1.1
  youtube_explode_dart: ^1.6.1+1
  video_player: ^0.11.1+2


dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true